# RpiTemps
Raspberry Pi temps display script

Prerequisits/first-time setup:
-----------------------------------
chmod +x disp-temps.sh

sudo usermod -aG video [username]

Then log out and back in

Use
-----------------------------------
Run this script with:

./disp-temps.sh

to display the temp of the CPU and GPU of a raspberry pi.
